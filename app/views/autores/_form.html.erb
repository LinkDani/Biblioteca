<%= form_for(@autore) do |f| %>
  <% if @autore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@autore.errors.count, "error") %> prohibited this autore from being saved:</h2>

      <ul>
      <% @autore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.select :item_id, Item.all.collect {|item| [item.name, item.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
